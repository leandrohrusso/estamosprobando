# Technical Debt log · `docs/logs/technical-debt.md`

> **Bitácora viva de deudas técnicas del proyecto.** Cada deuda detectada fuera del scope del PRP/tarea/sesión actual se registra acá en el acto (regla #24 [`register-out-of-scope-as-dt.md`](../../.claude/rules/register-out-of-scope-as-dt.md) · 8 campos contractuales). El skill [`/auditar-dt`](../../.claude/skills/auditar-dt/SKILL.md) audita mensualmente las DTs activas cruzadas con el roadmap próximo.

## Formato de cada fila (8 campos contractuales)

```markdown
| ID | Síntoma | Archivo/área | PRP destino | Severidad | Mitigación | Disparador | Sesión |
|---|---|---|---|---|---|---|---|
| <DT-NNN> | <síntoma 1-2 frases> | <path/área> | <PRP-NNN o ad-hoc futuro> | critical/normal/nit | <workaround o "ninguna"> | <condición objetiva para cerrar> | <commit hash o referencia> |
```

## DTs Activas

_(vacío al boot · adopter llena conforme detecte deudas fuera del scope del PRP/tarea/sesión actual · regla #24 [`register-out-of-scope-as-dt.md`](../../.claude/rules/register-out-of-scope-as-dt.md))_

| ID | Síntoma | Archivo/área | PRP destino | Severidad | Mitigación | Disparador | Sesión |
|---|---|---|---|---|---|---|---|
| DT-002 | El reset de `scripts/test-migrations.sh` (`DROP SCHEMA public CASCADE`) elimina el event-trigger Supabase `ensure_rls` + su función `rls_auto_enable` (viven en `public`) de la TEST DB. La red de seguridad de auto-habilitación de RLS desaparece tras cada corrida de test-migrations. | `scripts/test-migrations.sh` (reset_schema) + TEST DB Supabase | mini-PRP infra / ad-hoc futuro | normal | RLS explícito en cada tabla del PRP (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY`) + invariante `tests/sql/rls-invariants.sql` verde atrapa tablas con policy sin RLS · el auto-enable era defensa redundante | Endurecer `test-migrations.sh` (excluir/recrear `ensure_rls` en el reset) · O cuando un PRP futuro dependa del auto-enable | PRP-002 · Fase 1 |
| DT-003 | El happy-path e2e de G5 (login `form → "Revisá tu email"`) hace un envío real de magic link, no automatizable de forma estable en CI: el SMTP built-in de Supabase rate-limitea a ~2/hora (`429 over_email_send_rate_limit`) y rechaza el TLD `.test` (`email_address_invalid`). | `tests/e2e/regression/PRP-002-auth-onboarding.spec.ts` (G5) + config email del proyecto Supabase | PRP de TASK-008 (Resend) | normal | Spec G5 automatizado cubre validación de form + estructura (determinístico) · el pipeline de auth end-to-end lo prueba G4 vía el route real `/auth/confirm` · el happy-path "form→enviado" se cubre en el smoke visual de Fase 4 (Playwright MCP) + manual | Al integrar SMTP propio / Resend (TASK-008): re-habilitar el envío real en CI y automatizar la aserción del estado "Revisá tu email" | PRP-002 · Fase 2 (firma 🔵 user opción A) |
| DT-004 | `sendMagicLink` (endpoint público pre-auth) dispara `signInWithOtp` con cualquier email sin rate limiting a nivel app → vector de email-bombing / agotamiento de la cuota SMTP. La única defensa hoy es el rate-limit server-side propio de Supabase (por email+IP). | `src/app/(auth)/login/actions.ts` (`sendMagicLink`) + capa edge/proxy | PRP de TASK-008 (Resend) / mini-PRP infra edge | normal | Rate-limit server-side propio del proveedor (Supabase) por email+IP · DT-003 ya documenta el límite del SMTP built-in · impacto acotado a la protección del proveedor | Al cablear rate limiting de edge/proxy o al integrar Resend (TASK-008): agregar límite por IP+email a `/login` | LR-001 · PRP-002 · commit 647df90 |
| DT-005 | Tokens de tema claro del design system fallan contraste WCAG: borde de `--input` (#e6ddd3) vs Card (#fff) ≈1.36:1 (SC 1.4.11 pide 3:1 · input casi imperceptible) y texto blanco del botón primario sobre `--primary` (#c15c38) ≈4.32:1 (SC 1.4.3 pide 4.5:1 para texto normal · CTA marginalmente bajo el umbral). Afecta ambos flujos auth en tema claro (tema oscuro pasa). | `src/app/globals.css` (tokens `--input` L78 · `--primary` L58) | mini-PRP ajuste de tokens del DS / ad-hoc futuro | normal | ninguna (defecto presente en tema claro) | Al abrir sesión de ajuste de tokens del design system o próximo PRP que toque `globals.css` (cambio cross-cutting · afecta toda la app · fuera del scope auth de PRP-002 · el token vive en PRP-001, no en el diff de PRP-002) | LR-001 · PRP-002 · commit 647df90 |
| DT-006 | Invariante slug-inmutable (SD-cos-8) vive SOLO en Server Actions: la policy `org_update` es `FOR UPDATE` sin restricción de columna + `GRANT UPDATE` column-wide → un Owner con su JWT legítimo podría mutar `organizations.slug` vía PostgREST directo (una policy RLS no puede imponer inmutabilidad de columna). Latente: no existe caller UPDATE de organizations hoy. | `db/migrations/0002_rls_and_helpers.sql:68-72` (`org_update`) + prosa SD-cos-8 del PRP-002 | primer PRP con superficie de edición de organización / ad-hoc futuro | normal | ninguna caller UPDATE de organizations existe hoy · cero superficie de edición de org en PRP-002 | Al implementar la primera superficie de edición de org: `GRANT UPDATE (col,...)` column-level sin `slug` o trigger `BEFORE UPDATE` que rechace cambio de slug + corregir la prosa SD-cos-8 | LR-002 · PRP-002 · commit 9a70250 |
| DT-007 | Todo el copy de UI usa voseo rioplatense ("Revisá","Ingresá","Creá","Elegí","Pertenecés","Gestioná") aplicado consistentemente · no existe regla de copy-localization/vocabulario que fije voseo (es-AR) vs español neutro LATAM como canon del producto · decisión de voz de marca sin firmar (la convención "neutro" que asumió el agente i18n NO está declarada en BUSINESS_LOGIC ni en `docs/product/references/rules/`). | Copy de UI (login/onboarding/select-org/dashboard/members pages + error strings) · `src/lib/auth/roles.ts` (roleLabel mezcla 'Dueño'/'Administrador' con 'Staff') | mini-PRP de vocabulario/copy del producto / ad-hoc futuro | normal | voseo aplicado de forma consistente en toda la superficie auth (coherente si es voz de marca deliberada · cero bug funcional) | Cuando el user firme la convención de copy (voseo es-AR vs neutro LATAM): codificar en `docs/product/references/rules/vocabulario.md` + alinear copy + resolver 'Staff' vs 'Dueño'/'Administrador' | LR-002 · PRP-002 · commit 9a70250 |
| DT-008 | Controles táctiles de los primitivos del DS bajo la barra de 44×44px del spec del proyecto (button default h-10=40px · sm h-9=36px · botón "Quitar" sm 36px · selects h-9/h-10) en superficies que colapsan a mobile. Cumple WCAG AA 2.5.8 (≥24px) pero no la barra 44px del spec (equivalente AAA 2.5.5). | `src/components/ui/button.tsx:19-22` · `input.tsx:13` · `members-manager.tsx:64,122` · `org-switcher.tsx:27` | sesión de calibración de sizing del DS / matriz Claude Design / ad-hoc futuro | normal | cumple WCAG AA 2.5.8 (≥24px) · el gap es contra la barra 44px del spec del proyecto (no bloquea AA) | Al abrir sesión de ajuste de sizing del DS o matriz Claude Design (cambio cross-cutting · afecta todos los botones app-wide · decisión de diseño fuera del scope auth de PRP-002) | LR-002 · PRP-002 · commit 9a70250 |

## DTs Resueltas

_(vacío al boot · mover filas acá cuando un PRP cierre la deuda · agregar commit hash de resolución)_

| ID | Síntoma | Resuelta por | Commit |
|---|---|---|---|
| DT-001 | Inconsistencia de var de conexión a TEST DB: `run-sql-tests.sh` leía `DATABASE_URL` mientras sus hermanos usaban `TEST_DATABASE_URL`. | PRP-002 Fase 1 · unificado a `TEST_DATABASE_URL` en `scripts/run-sql-tests.sh` (guard skip-safe + psql call + comentarios) | PRP-002 · commit fase 1 |

---

_Bitácora gestionada por reglas #24 [`register-out-of-scope-as-dt.md`](../../.claude/rules/register-out-of-scope-as-dt.md) + #18 [`golden-rule-docs-memory.md`](../../.claude/rules/golden-rule-docs-memory.md) ítem 4.6 + skill [`/auditar-dt`](../../.claude/skills/auditar-dt/SKILL.md)._
