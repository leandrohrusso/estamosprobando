{
  "_instrucciones": "Template documental de la estructura del archivo `.credentials.local.json` que usa el skill `/validar` (paso 5 del flujo de 6 pasos) para login runtime contra la app real con MCPs del stack del adopter. Este `.test-template` NO se gitignora (sirve de referencia de shape para devs nuevos) · el archivo real `.credentials.local.json` con valores SÍ se gitignora (ver `.gitignore` raíz). Cómo usar: (1) copiar este archivo a `.credentials.local.json` (mismo path · sin sufijo `.test-template`) · (2) reemplazar los placeholders por valores reales de tu BD test · (3) NUNCA commitear el archivo real · (4) si tu CI corre el suite contra BD test, cargar el JSON real como secret del CI (ej: `TEST_CREDENTIALS_JSON` en GitHub Actions). STACK ADAPTATION: este template asume backend con auth multi-user y opcionalmente multi-tenant (org/tenant/account). Si tu stack es single-user o tiene shape de auth distinto (ej: API keys en lugar de email+password · OAuth-only · JWT preminted), adaptá los campos · el principio (credentials runtime para `/validar` · cero secrets en git · password uniforme test por diseño) es universal.",
  "_backend": {
    "provider": "<ej: supabase | firebase | postgres-self-hosted | mongodb-atlas | etc>",
    "project_ref": "<id/ref del proyecto BD test>",
    "url": "<URL del backend test · ej: https://<ref>.supabase.co>",
    "region": "<region del backend · ej: us-east-1>",
    "creado": "<YYYY-MM-DD · fecha de creación del proyecto BD test>"
  },
  "users": [
    {
      "label": "<rol-1-slug>",
      "email": "<rol-1>@<tenant-slug>.test",
      "password": "test-password-123",
      "role": "<rol del producto · adopter define · ej: admin | owner | member | viewer | etc>",
      "user_id": "11111111-1111-4111-8111-111111111111",
      "tenant_slug": "<opcional · solo si multi-tenant · slug del tenant/org al que pertenece este user>",
      "notes": "<por qué existe este user en el seed · qué specs/casos del CSV lo usan · qué shape de permisos tiene>"
    },
    {
      "label": "<rol-2-slug>",
      "email": "<rol-2>@<tenant-slug>.test",
      "password": "test-password-123",
      "role": "<rol del producto>",
      "user_id": "22222222-2222-4222-8222-222222222222",
      "tenant_slug": "<opcional>",
      "notes": "<por qué existe>"
    },
    {
      "label": "<rol-3-slug · opcional · ej: tenant secundario para tests de aislamiento multi-tenant>",
      "email": "<rol-3>@<tenant-2-slug>.test",
      "password": "test-password-123",
      "role": "<rol del producto>",
      "user_id": "33333333-3333-4333-8333-333333333333",
      "tenant_slug": "<tenant-2-slug · independiente del anterior>",
      "notes": "Tenant independiente para tests de aislamiento RLS multi-tenant · ningún dato del tenant-1 debe ser visible desde acá."
    }
  ],
  "superAdmin": {
    "_aplica_si": "El producto tiene rol super-admin global (cross-tenant). Eliminar este bloque si NO aplica al adopter.",
    "email": "<super-admin-email>",
    "password": "test-password-123",
    "user_id": "00000000-0000-4000-8000-000000000000",
    "notes": "<cómo se materializa el flag super-admin en el seed · ej: flag en tabla settings · column boolean · etc>"
  },
  "_password_policy": "Todas las passwords del seed test son uniformes (`test-password-123`) por diseño · NO usar passwords reales de prod en TEST · si una password no funciona durante un spec, regenerar el seed (`<comando del stack del adopter · ej: psql $TEST_DATABASE_URL -f db/seeds/test-seed.sql>`).",
  "_cross_refs": {
    "skill_validar": "`.claude/skills/validar/SKILL.md` · skill que consume este archivo en runtime",
    "regla_seed_upsert": "`.claude/rules/seed-upsert-with-fixed-id.md` · convención de seed durable con UUIDs fijos (paridad con `user_id` placeholder de este template)",
    "regla_register_dt": "`.claude/rules/register-out-of-scope-as-dt.md` · si el adopter detecta que falta campo del template para su stack, abrir DT en lugar de improvisar inline"
  }
}
