{
  "name": "PROJECT_NAME_PLACEHOLDER",
  "version": "0.0.1",
  "description": "TODO · 1 línea con qué es el producto",
  "private": true,
  "scripts": {
    "typecheck": "echo 'TODO: replace with project typecheck command, e.g.: tsc --noEmit'",
    "lint": "echo 'TODO: replace with project lint command, e.g.: eslint .'",
    "build": "echo 'TODO: replace with project build command, e.g.: next build'",
    "test": "npm run test:unit",
    "test:unit": "echo 'TODO: replace with project unit test command, e.g.: vitest run'",
    "test:e2e": "echo 'TODO: replace with project E2E test command, e.g.: playwright test'",
    "ci:local": "bash scripts/local-ci.sh",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  },
  "dependencies": {},
  "comment": "TODO al bootstrap del proyecto: (1) reemplazar PROJECT_NAME_PLACEHOLDER con el nombre real, (2) actualizar description, (3) reemplazar los TODO de cada script con el comando real del stack, (4) sumar devDependencies (husky + lint-staged + tooling de lint/test/build) · ⚠️ husky + lint-staged tienen que estar en devDependencies ANTES del primer 'npm install' del bootstrap · sin ellos el script 'prepare: husky' rompe con 'sh: husky: not found' exit 127, (5) sumar dependencies del producto. El script 'prepare: husky' activa los hooks de .husky/ al correr 'npm install'."
}
